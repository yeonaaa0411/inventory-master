# Use the official PHP image with Apache from Docker Hub
FROM php:8.1-apache

# Set the working directory inside the container
WORKDIR /var/www/html

# Copy the current directory contents into the container at /var/www/html
COPY . .

# Install necessary PHP extensions
RUN docker-php-ext-install mysqli pdo pdo_mysql

# Set appropriate permissions for the web server user
RUN chown -R www-data:www-data /var/www/html

# Enable the Apache mod_rewrite module for URL rewriting
RUN a2enmod rewrite

# Expose port 80 to the outside world
EXPOSE 80

# Optional: Set the command to run when starting the container
CMD ["apache2-foreground"]
